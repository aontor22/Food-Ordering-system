VITE_API_URL=/api
# Must match server PAYMENT_CURRENCY; changing it does not convert prices.
VITE_CURRENCY=BDT

# Google OAuth Web client ID. Use the same value as server GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
